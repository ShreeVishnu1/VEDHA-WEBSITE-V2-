<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Welcome, <%= user.Username %>!</h1>
    <h2>Your Booked Slots:</h2>
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Time</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            <% slots.forEach(slot => { %>
                <tr>
                    <td><%= slot.Date %></td>
                    <td><%= slot.Time %></td>
                    <td><%= slot.Status %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <br>
    <a href="/book">Book a New Slot</a>
    <br>
    <a href="/logout">Logout</a>
</body>
</html>
